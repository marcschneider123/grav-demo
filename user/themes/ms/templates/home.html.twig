{% extends 'layouts/base.html.twig' %}

{% block content %}

	<div class="container">
{#		<div class="row text-center">#}
{#			<div class="col-sm-5">{{ page.media['logo.png'].html('', 'Header logo', 'img-fluid') }}</div>#}
{#			<div class="col-sm-7">{{ page.media['header.png'].html('', 'Header', 'img-fluid') }}</div>#}
{#		</div>#}
		<div class="row">
			<div class="col-12">
				<h1>{{ page.title }}</h1>
				{{ page.content }}
			</div>
		</div>
	</div>
{% endblock %}
