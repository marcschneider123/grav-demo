<header class="masthead">
	<div class="container h-100">
		<div class="row h-100 d-flex align-content-between flex-wrap">
			<div class="col-12 text-center title">
				<h1 class="d-none d-md-block">
					<a href="#services" class="scroll">
						{#					{{ media['user://themes/zwohm/images/logo.png'].html('Titel', 'alt text', 'img-fluid') }}#}
						{{ page.media['desktop.jpg'].html(page.header.title, page.header.title, 'img-fluid') }}
					</a>
				</h1>
				<h1 class="d-block d-md-none">
					<a href="#services" class="scroll">
						{{ page.media['mobile.jpg'].html(page.header.title, page.header.title, 'img-fluid') }}
					</a>
				</h1>
			</div>
			<div class="col-md-8 offset-md-2 col-lg-6 offset-lg-3">
				<a href="#" class="btn btn-primary btn-lg btn-block" title="{{ page.header.button }}">
					<span>{{ page.header.button }}</span>
				</a>
			</div>
			<div class="col-12 text-center pb-3">
				{#				@include('_partials.scrollDown')#}
			</div>
		</div>
	</div>
</header>
